import 'package:flutter/material.dart';

class GroupReportPage extends StatelessWidget {
  const GroupReportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
