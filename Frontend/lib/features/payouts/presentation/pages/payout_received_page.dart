import 'package:flutter/material.dart';

class PayoutReceivedPage extends StatelessWidget {
  const PayoutReceivedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
