import 'package:flutter/material.dart';

class PayoutSchedulePage extends StatelessWidget {
  const PayoutSchedulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
