import 'package:flutter/material.dart';

class PayoutDetailPage extends StatelessWidget {
  const PayoutDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
