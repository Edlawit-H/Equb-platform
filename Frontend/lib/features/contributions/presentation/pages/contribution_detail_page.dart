import 'package:flutter/material.dart';

class ContributionDetailPage extends StatelessWidget {
  const ContributionDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
