import 'package:flutter/material.dart';

class PayContributionPage extends StatelessWidget {
  const PayContributionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
